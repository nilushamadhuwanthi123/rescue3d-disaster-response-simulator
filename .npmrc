# Some contributors' shells set NODE_ENV=production globally, which makes
# npm silently omit devDependencies (tsc, vitest, eslint, husky, ...) on
# `npm install`. Force dev deps to always install for this repo.
include=dev
